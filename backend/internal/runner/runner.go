// Package runner — запуск приложения: загрузка конфига, подключение к БД, применение миграций (если делаешь из кода), запуск HTTP и опционально WebSocket.
//
// Ожидаемые функции:
//
// - Run(cfg config.Config) error — принимает конфиг, поднимает БД, создаёт репозитории/сервисы/хендлеры, запускает http.Server (и при необходимости отдельный listener для WebSocket),
//   отдаёт ошибку при падении сервера. Обычно из main вызывается после config.Load().
//
// Можно обойтись без этого пакета и делать всё в main.go (инициализация и ListenAndServe).
package runner
